% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forest_table.R
\name{forest_table}
\alias{forest_table}
\title{Extract a formatted effect-size table without generating a plot}
\usage{
forest_table(
  data,
  outcome,
  exposure,
  covariates = NULL,
  model_type = "logistic",
  cumulative = FALSE,
  cumulative_labels = NULL,
  conf_level = 0.95,
  time_var = NULL,
  event_var = NULL,
  strata = NULL,
  cluster = NULL,
  weights = NULL,
  table_digits = 2
)
}
\arguments{
\item{data}{A data frame containing all variables.}

\item{outcome}{Character string. Name of the outcome variable (ignored for
Cox models - use \code{time_var} and \code{event_var} instead).}

\item{exposure}{Character string. Name of the exposure variable of interest.}

\item{covariates}{Character vector of confounder/covariate names. In
non-cumulative mode all covariates are added together; in cumulative mode
they are added one at a time in the order supplied. Default \code{NULL} produces
only the unadjusted estimate.}

\item{model_type}{Character. One of \code{"logistic"} (default), \code{"linear"},
\code{"poisson"}, or \code{"coxph"}.}

\item{cumulative}{Logical. If \code{TRUE}, fit models that progressively add one
covariate at a time and show each step as a separate row. Default \code{FALSE}.

\strong{Important:} Odds ratios (\code{"logistic"}) and hazard ratios (\code{"coxph"}) are
\strong{non-collapsible} effect measures. This means the exposure coefficient
will change as covariates are added even in the complete absence of
confounding, because adding covariates reduces residual variance on the
latent scale. Consequently, a shifting OR or HR across sequential models
cannot be cleanly attributed to confounding. For causal inference, the
unadjusted vs. fully-adjusted comparison (the default) is preferred.
Cumulative display is most interpretable for collapsible measures:
risk differences (\code{"linear"}) and risk ratios (\code{"poisson"}).}

\item{cumulative_labels}{Optional named character vector to rename the
cumulative model labels. Names should match the auto-generated labels
(e.g., \code{"+ age"}, \code{"+ age + sex"}); values are the replacement labels.}

\item{conf_level}{Numeric. Confidence level for intervals. Default \code{0.95}.}

\item{time_var}{Character. Name of the time variable (Cox model only).}

\item{event_var}{Character. Name of the event indicator variable (Cox model
only; should be 0/1 or logical).}

\item{strata}{Character. Name of a stratification variable for Cox models.
Default \code{NULL}.}

\item{cluster}{Character. Name of a clustering variable for cluster-robust
standard errors. Requires the \pkg{sandwich} and \pkg{lmtest} packages.
Default \code{NULL}.}

\item{weights}{Character. Name of a survey/frequency weight variable.
Default \code{NULL}.}

\item{table_digits}{Integer. Number of decimal places in the table. Default
\code{2}.}
}
\value{
A data frame with columns:
\describe{
\item{\code{model}}{Row label (e.g., "Unadjusted", "Adjusted").}
\item{\code{estimate}}{Point estimate (formatted character).}
\item{\code{ci}}{Confidence interval as a character string (e.g.,
\code{"0.95–1.42"}).}
\item{\code{formatted}}{Combined estimate and CI (e.g.,
\code{"1.15 (0.95–1.42)"}).}
\item{\code{p.value}}{Formatted p-value character string.}
\item{\code{n}}{Number of observations.}
}
}
\description{
A convenience wrapper around \code{gg_adjusted_forest()} that returns only the
formatted table. Useful when you want numeric summaries without producing a
graphic.
}
\examples{
data(mtcars)
mtcars$am <- as.integer(mtcars$am)
forest_table(
  data       = mtcars,
  outcome    = "am",
  exposure   = "hp",
  covariates = c("wt", "cyl"),
  model_type = "logistic"
)
}
