% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gg_adjusted_forest.R
\name{gg_adjusted_forest}
\alias{gg_adjusted_forest}
\title{Forest plot of unadjusted and adjusted effects for a single exposure}
\usage{
gg_adjusted_forest(
  data,
  outcome,
  exposure,
  covariates = NULL,
  model_type = "logistic",
  cumulative = FALSE,
  cumulative_labels = NULL,
  effect_label = NULL,
  title = NULL,
  ref_line = NULL,
  point_size = 4,
  point_shape = 15,
  line_size = 0.7,
  color = "black",
  vline_color = "grey50",
  vline_linetype = "dashed",
  x_limits = NULL,
  x_breaks = NULL,
  log_scale = TRUE,
  conf_level = 0.95,
  time_var = NULL,
  event_var = NULL,
  strata = NULL,
  cluster = NULL,
  weights = NULL,
  show_table = TRUE,
  table_digits = 2
)
}
\arguments{
\item{data}{A data frame containing all variables.}

\item{outcome}{Character string. Name of the outcome variable (ignored for
Cox models - use \code{time_var} and \code{event_var} instead).}

\item{exposure}{Character string. Name of the exposure variable of interest.}

\item{covariates}{Character vector of confounder/covariate names. In
non-cumulative mode all covariates are added together; in cumulative mode
they are added one at a time in the order supplied. Default \code{NULL} produces
only the unadjusted estimate.}

\item{model_type}{Character. One of \code{"logistic"} (default), \code{"linear"},
\code{"poisson"}, or \code{"coxph"}.}

\item{cumulative}{Logical. If \code{TRUE}, fit models that progressively add one
covariate at a time and show each step as a separate row. Default \code{FALSE}.}

\item{cumulative_labels}{Optional named character vector to rename the
cumulative model labels. Names should match the auto-generated labels
(e.g., \code{"+ age"}, \code{"+ age + sex"}); values are the replacement labels.}

\item{effect_label}{Character. X-axis label. Defaults to \code{"Odds Ratio (95 \\\% CI)"} for logistic, \code{"Risk Ratio (95 \\\% CI)"} for Poisson,
\code{"Hazard Ratio (95 \\\% CI)"} for Cox, and \code{"Coefficient (95 \\\% CI)"} for
linear.}

\item{title}{Character. Plot title. Default \code{NULL} (no title).}

\item{ref_line}{Numeric. Position of the vertical reference line. Defaults
to \code{1} for ratio models and \code{0} for linear.}

\item{point_size}{Numeric. Size of the point estimate symbol. Default \code{4}.}

\item{point_shape}{Integer. ggplot2 shape code. Default \code{15} (filled square).}

\item{line_size}{Numeric. Thickness of the CI lines. Default \code{0.7}.}

\item{color}{Character. Colour for points and CI lines. Default \code{"black"}.}

\item{vline_color}{Character. Colour of the reference line. Default
\code{"grey50"}.}

\item{vline_linetype}{Character. Linetype of the reference line. Default
\code{"dashed"}.}

\item{x_limits}{Numeric vector of length 2. Manual x-axis limits. Default
\code{NULL} (automatic).}

\item{x_breaks}{Numeric vector. Manual x-axis break positions. Default
\code{NULL} (automatic).}

\item{log_scale}{Logical. Use log scale on the x-axis for ratio models.
Default \code{TRUE}.}

\item{conf_level}{Numeric. Confidence level for intervals. Default \code{0.95}.}

\item{time_var}{Character. Name of the time variable (Cox model only).}

\item{event_var}{Character. Name of the event indicator variable (Cox model
only; should be 0/1 or logical).}

\item{strata}{Character. Name of a stratification variable for Cox models.
Default \code{NULL}.}

\item{cluster}{Character. Name of a clustering variable for cluster-robust
standard errors. Requires the \pkg{sandwich} and \pkg{lmtest} packages.
Default \code{NULL}.}

\item{weights}{Character. Name of a survey/frequency weight variable.
Default \code{NULL}.}

\item{show_table}{Logical. Combine the forest plot with a formatted table
panel (using \pkg{patchwork}). Default \code{TRUE}.}

\item{table_digits}{Integer. Number of decimal places in the table. Default
\code{2}.}
}
\value{
An object of class \code{ggadjustedforest} (a list) with components:
\describe{
\item{\code{plot}}{The combined \pkg{ggplot2}/\pkg{patchwork} plot object.
When \code{show_table = FALSE} this is just the forest plot.}
\item{\code{table}}{A data frame with columns \code{model}, \code{estimate},
\code{conf.low}, \code{conf.high}, \code{p.value}, and \code{n}.}
\item{\code{formatted_table}}{A data frame with a \code{formatted} column
containing strings like \code{"1.23 (1.01-1.55)"}.}
}
}
\description{
Creates a publication-quality forest plot showing only the unadjusted and
adjusted (or cumulatively adjusted) effect estimates for a specified exposure
variable, hiding confounder coefficients in accordance with causal inference
principles.
}
\examples{
# Logistic regression example
data(mtcars)
mtcars$am <- as.integer(mtcars$am)   # binary outcome
result <- gg_adjusted_forest(
  data        = mtcars,
  outcome     = "am",
  exposure    = "hp",
  covariates  = c("wt", "cyl"),
  model_type  = "logistic",
  title       = "Effect of Horsepower on Transmission Type"
)
result$table

# Cumulative adjustment
result2 <- gg_adjusted_forest(
  data       = mtcars,
  outcome    = "am",
  exposure   = "hp",
  covariates = c("wt", "cyl"),
  cumulative = TRUE
)
result2$table
}
