% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gg_multi_outcome_forest.R
\name{gg_multi_outcome_forest}
\alias{gg_multi_outcome_forest}
\title{Forest plot comparing effects across multiple outcomes}
\usage{
gg_multi_outcome_forest(
  data = NULL,
  outcomes = NULL,
  exposure = NULL,
  covariates = NULL,
  model_type = "logistic",
  cumulative = FALSE,
  cumulative_labels = NULL,
  outcome_list = NULL,
  conf_level = 0.95,
  time_var = NULL,
  event_var = NULL,
  strata = NULL,
  cluster = NULL,
  weights = NULL,
  effect_label = NULL,
  title = NULL,
  ref_line = NULL,
  point_size = 4,
  point_shape = 15,
  line_size = 0.7,
  color = "black",
  vline_color = "grey50",
  vline_linetype = "dashed",
  x_limits = NULL,
  x_breaks = NULL,
  log_scale = TRUE,
  free_x = FALSE,
  strip_position = "right",
  show_table = TRUE,
  table_digits = 2
)
}
\arguments{
\item{data}{A data frame. Required for simple mode; ignored in advanced mode.}

\item{outcomes}{Named character vector mapping display labels to column names,
e.g. \code{c("30-day Mortality" = "mort_30d", "Failure to Rescue" = "ftr")}.
Used only in simple mode.}

\item{exposure}{Character string. Name of the exposure variable. Used only
in simple mode.}

\item{covariates}{Character vector of confounder names shared across all
outcomes. Used only in simple mode. Default \code{NULL}.}

\item{model_type}{Character. One of \code{"logistic"} (default), \code{"linear"},
\code{"poisson"}, or \code{"coxph"}. A single value is applied to all outcomes.
Used only in simple mode.}

\item{cumulative}{Logical. Passed to each \code{gg_adjusted_forest()} call in
simple mode. Default \code{FALSE}.}

\item{cumulative_labels}{Optional named character vector. Passed to each
\code{gg_adjusted_forest()} call in simple mode.}

\item{outcome_list}{Named list of \code{ggadjustedforest} objects (the return
value of \code{gg_adjusted_forest()}).  Used only in advanced mode.  Names
become the facet strip labels.}

\item{conf_level}{Numeric. Confidence level for intervals. Default \code{0.95}.
Used only in simple mode.}

\item{time_var}{Character. Time variable name for Cox models. Simple mode.}

\item{event_var}{Character. Event indicator variable name for Cox models.
Simple mode.}

\item{strata}{Character. Stratification variable for Cox models. Simple mode.}

\item{cluster}{Character. Clustering variable for robust SEs. Simple mode.}

\item{weights}{Character. Survey weight variable name. Simple mode.}

\item{effect_label}{Character. X-axis label. Auto-generated if \code{NULL}.}

\item{title}{Character. Overall plot title. Default \code{NULL}.}

\item{ref_line}{Numeric. Reference line position. Defaults to \code{1} for ratio
models and \code{0} for linear.}

\item{point_size}{Numeric. Size of the point symbol. Default \code{4}.}

\item{point_shape}{Integer. ggplot2 shape code. Default \code{15} (filled square).}

\item{line_size}{Numeric. CI line thickness. Default \code{0.7}.}

\item{color}{Character. Colour for points and CI lines. Default \code{"black"}.}

\item{vline_color}{Character. Reference line colour. Default \code{"grey50"}.}

\item{vline_linetype}{Character. Reference line linetype. Default
\code{"dashed"}.}

\item{x_limits}{Numeric vector of length 2. Manual x-axis limits. Default
\code{NULL}.}

\item{x_breaks}{Numeric vector. Manual x-axis break positions. Default
\code{NULL}.}

\item{log_scale}{Logical. Use log scale for ratio models. Default \code{TRUE}.}

\item{free_x}{Logical. Allow each facet to have its own x-axis scale.
Default \code{FALSE} (shared scale for comparability).}

\item{strip_position}{Character. Where to place outcome strip labels:
\code{"right"} (default) or \code{"left"}.}

\item{show_table}{Logical. Attach a formatted table panel. Default \code{TRUE}.}

\item{table_digits}{Integer. Decimal places in table. Default \code{2}.}
}
\value{
An object of class \code{ggadjustedforest_multi} (a list) with:
\describe{
\item{\code{plot}}{The combined \pkg{ggplot2}/\pkg{patchwork} plot.}
\item{\code{tables}}{A named list of \code{formatted_table} data frames, one per
outcome.}
\item{\code{combined_table}}{A single data frame with all outcomes stacked,
including an \code{outcome} column.}
\item{\code{results}}{A named list of the underlying \code{ggadjustedforest}
objects.}
}
}
\description{
Runs \code{gg_adjusted_forest()} for each outcome and combines the results into
a single faceted forest plot, with one facet panel per outcome.  Two usage
modes are supported:
}
\details{
\itemize{
\item \strong{Simple mode} (supply \code{outcomes}, \code{exposure}, and optional \code{covariates}):
all outcomes share the same exposure, covariates, and model type.
\item \strong{Advanced mode} (supply \code{outcome_list}): pass a named list of objects
returned by \code{gg_adjusted_forest()}, giving full per-outcome control over
covariates, model type, and cumulative adjustment.
}
}
\examples{
data(mtcars)
mtcars$am  <- as.integer(mtcars$am)
mtcars$vs  <- as.integer(mtcars$vs)

# Simple mode: two binary outcomes, same exposure and covariates
result <- gg_multi_outcome_forest(
  data       = mtcars,
  outcomes   = c("Transmission" = "am", "Engine Shape" = "vs"),
  exposure   = "hp",
  covariates = c("wt", "cyl"),
  model_type = "logistic",
  show_table = FALSE
)
result$combined_table

# Advanced mode: different covariate sets per outcome
r1 <- gg_adjusted_forest(mtcars, "am", "hp", c("wt"),       show_table = FALSE)
r2 <- gg_adjusted_forest(mtcars, "vs", "hp", c("wt", "cyl"), show_table = FALSE)
result2 <- gg_multi_outcome_forest(
  outcome_list = list("Transmission" = r1, "Engine Shape" = r2),
  show_table   = FALSE
)
}
